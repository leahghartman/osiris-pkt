simulation
{
  algorithm = "cuda",
}

node_conf
{
  node_number(1:2) = 1, 1,
  n_threads = 1,
  tile_number(1:2) = 64, 32,
  if_periodic(1:2) = .true., .true.,
  tile_topology = "hilbert",
}

grid
{
  nx_p(1:2) = 1024, 1024,
  coordinates = "cartesian",
}

time_step
{
  dt = 0.05,
  ndump = 10,
}

restart
{
}

space
{
  xmin(1:2) = 0, 0,
  xmax(1:2) = 102.4, 102.4,
  if_move = .false., .false.,
}

time
{
  tmin = 0.0,
  tmax = 5.0,
}

el_mag_fld
{
}

emf_bound
{
}

diag_emf
{
  ndump_fac = 1,
  reports = "e1",
}

particles
{
  num_species = 1,
  interpolation = "linear",
}

species
{
  name = "electrons",
  num_par_max = 180000,
  rqm = -1.0,
  num_par_x(1:2) = 8, 4,
  n_sort = 0,
  push_type = "standard",
}

udist
{
  uth(1:3) = 0.1, 0.1, 0.1,
  ! uth(1:3) = 0.0, 0.0, 0.0,
  ufl(1:3) = 0.0, 0.0, 0.0,
}

profile
{
  density = 1,
  profile_type = "uniform",
}

spe_bound
{
}

diag_species
{
}

current
{
}

diag_current
{
}

smooth
{
}
