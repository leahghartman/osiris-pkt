simulation
{
  algorithm = "qed",
  omega_p0 = 1.8836e15, ! we normalise to the laser wavelength = 1.0 microns
}

node_conf
{
  node_number(1:2) =  2, 2,
  if_periodic(1:2) = .true., .true.,
}


!----------spatial grid----------
grid
{
  nx_p(1:2) = 200, 200,
  coordinates = "cartesian",
}

!----------time step and global data dump timestep number----------
time_step
{
  dt =  0.021,
  ndump =  60,
}

!----------spatial limits of the simulations----------
space
{
  xmin(1:2) = -4.0, -4.0,
  xmax(1:2) =  4.0,  4.0,
  if_move(1:2) = .false., .false.,
}

!----------time limits ----------
time
{
  tmin =  0.00,
  tmax = 12.60,
}

el_mag_fld
{
  ext_fld = "static",
  type_ext_b(1:3) = "uniform", "uniform", "uniform",
  ext_b0(1:3) = 0.0, 0.0, 4.528d2,
}

!----------boundary conditions for em-fields ----------
emf_bound
{
  type(1:2,1) = "open","open",
  type(1:2,2) = "open","open",
}

!----------emf diagnostics----------
diag_emf{
  ndump_fac = 1,
  ndump_fac_ene_int = 1,
  reports = "ext_b1",
}

!----------number of particle species----------
particles
{
  num_qed = 1,
  interpolation = "cubic",
}

!----------qed group, setting photon and pair creation----------
qed_group
{
  if_damp_classical = .false.,
  if_damp_qed = .true.,
  if_pairprod = .false.,
  qed_g_cutoff =  0.,
  p_emit_cutoff = 1.0e10,
}

!----------qed group, setting diagnostics----------
qed_group_diag
{
  ndump_fac_rad = 1,

  ndump_fac_radspect = 1,
  radspect_emin = 1.0d-5,
  radspect_emax = 1.0d3,
  radspect_bins = 400,
}

!----------information for electrons----------

species
{
  name = "electrons",
  num_par_x(1:2) = 2, 5,
  num_par_max = 1000000,
  rqm = -1.0,
  push_type = "exact",
  iter_tol = 1.0d-3,
  rad_react = .true.,
  q_real = -1.0,
  add_tag = .true.,
}

udist
{
  uth(1:3) =     0.0, 0.0, 0.0,
  ufl(1:3) = 9.055d2, 0.0, 0.0,
}

profile
{
  density = 1.0d-8,
  profile_type(1:2)  = "gaussian", "gaussian",
  gauss_center(1:2)  =  0.00,  2.00,
  gauss_sigma(1:2)   =  0.08,  0.08,
  gauss_range(1:2,1) = -0.20,  0.20,
  gauss_range(1:2,2) =  1.80,  2.20,
}

spe_bound
{
  type(1:2,1) = "open","open",
  type(1:2,2) = "open","open",
}

diag_species
{
  ndump_fac = 1,
  ndump_fac_ene = 1,
  ndump_fac_raw = 1,
  ndump_fac_pha = 1,
  reports = "charge",

  ps_xmin(1:2) = -4.0, -4.0,
  ps_xmax(1:2) =  4.0,  4.0,
  ps_nx(1:2)   =  200,  200,

  ps_pmin(1:3)   = -1.e-5, -1.e-5, -1.e-5,
  ps_pmax(1:3)   =  1.e-5,  1.e-5,  1.e-5,
  ps_np(1:3)     =    400,    400,    400,
  if_ps_p_auto(1:3) = .true., .true., .true.,

  ps_ngamma   =  1000,
  ps_gammamin = 1.0e-5,
  ps_gammamax = 1.0e-5,
  if_ps_gamma_auto = .true.,

  phasespaces = "p2p1", "g",

  raw_fraction = 1.00,

}

!----------information for positrons----------

species
{
  name = "positrons",
  num_par_x(1:2) = 1, 1,
  num_par_max = 1000,
  rqm = 1.0,
  push_type="standard",
  add_tag = .true.,
}

udist
{
  uth(1:3) = 0.0, 0.0, 0.0,
  ufl(1:3) = 0.0, 0.0, 0.0,
}

profile
{
  density = 0.0,
}

spe_bound
{
  type(1:2,1) = "open","open",
  type(1:2,2) = "open","open",
}

diag_species
{
  ndump_fac = 1,
  ndump_fac_ene = 1,
  ndump_fac_raw = 1,
  ndump_fac_pha = 1,
  reports = "charge",

  ps_xmin(1:2) = -4.0, -4.0,
  ps_xmax(1:2) =  4.0,  4.0,
  ps_nx(1:2)   =  200,  200,

  ps_pmin(1:3)   = -1.e-5, -1.e-5, -1.e-5,
  ps_pmax(1:3)   =  1.e-5,  1.e-5,  1.e-5,
  ps_np(1:3)     =    400,    400,    400,
  if_ps_p_auto(1:3) = .true., .true., .true.,

  ps_ngamma   =  1000,
  ps_gammamin = 1.0e-5,
  ps_gammamax = 1.0e-5,
  if_ps_gamma_auto = .true.,

  phasespaces = "p2p1", "g",

  raw_fraction = 0.01,
}

!----------information for photons----------

species
{
  name = "photons",
  num_par_x(1:2) = 1, 1,
  num_par_max = 1000,
  push_type = "standard",
  add_tag = .true.,
}

udist
{
  uth(1:3) = 0.0, 0.0, 0.0,
  ufl(1:3) = 0.0, 0.0, 0.0,
}

profile
{
  density = 0.0,
}

spe_bound
{
  type(1:2,1) = "open","open",
  type(1:2,2) = "open","open",
}

diag_species
{
  ndump_fac = 1,
  ndump_fac_ene = 1,
  ndump_fac_raw = 1,
  ndump_fac_pha = 1,
  reports = "charge",

  ps_xmin(1:2) = -4.0, -4.0,
  ps_xmax(1:2) =  4.0,  4.0,
  ps_nx(1:2)   =  200,  200,

  ps_pmin(1:3)   = -1.e-5, -1.e-5, -1.e-5,
  ps_pmax(1:3)   =  1.e-5,  1.e-5,  1.e-5,
  ps_np(1:3)     =    400,    400,    400,
  if_ps_p_auto(1:3) = .true., .true., .true.,

  ps_ngamma   =  1000,
  ps_gammamin = 1.0e-5,
  ps_gammamax = 1.0e-5,
  if_ps_gamma_auto = .true.,

  phasespaces = "p2p1", "g",

  raw_fraction = 0.01,
}

! --------------------- end of osiris input file ---------------
